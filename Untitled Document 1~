git push origin master == for saving n pushing all to the master file
git commit -m "testing" == for giving some name with commiting
git add . == for adding all the files
ll == listing all the files
rf == re
rm == removing
mv .git .. == moves the git repostory
 git clone git@github.com:rugbyprof/ParkingSpaceIdentifier.git == for cloning 

